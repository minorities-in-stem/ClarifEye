import SwiftUI
import MetalKit
import Metal

struct SettingsView: View {
    @ObservedObject var settings: Settings
    
    var body: some View {
        List {
            Section(header: Text("Depth Settings")) {
                
                
               
            }
        }
    }
}
